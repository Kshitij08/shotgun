# Database populate scripts (temporary, contains sensitive data)
db-populate/

# Variable setup scripts (may contain sensitive data)
variable-setup/

# Documentation files in root directory only
/*.md

# MD files folder
md files/

# Keep README files in subdirectories (they're not ignored)

